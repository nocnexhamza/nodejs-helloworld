{
  "name": "nodejs-hello-world",
  "version": "1.0.0",
  "description": "Simple Node.js Hello World app for Kubernetes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
